## Ambiente0 – FastAPI + Docker + Kubernetes + CI (GitHub Actions)

API simples em Python (FastAPI) para demonstrar práticas DevOps:
- Testes automatizados (pytest)
- Containerização com Docker
- Deploy em Kubernetes (manifests)
- CI no GitHub Actions com build e push de imagem no GHCR

## Rodar local com Docker
```bash
docker compose up --build



