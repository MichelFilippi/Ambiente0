apiVersion: v1
kind: Service
metadata:
  name: portfolio-api-svc
  namespace: devops-portfolio
spec:
  selector:
    app: portfolio-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP